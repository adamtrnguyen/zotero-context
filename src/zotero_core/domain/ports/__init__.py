"""Ports: what the application layer needs, stated without naming who provides it.

Empty on purpose. Nothing is re-exported from a layer package — the ROOT `__init__.py`
is this package's one documented exception, because `__all__` there is the published API.
"""
